let a = 2;
// Объявляем переменную и инициализируем ее числом 2
let x = 1 + (a *= 2);
// вычисляем (a *= 2)
// *= - умножаем значение переменой "а" на число 2, результат 4 (теперь в переменной "a" хрранится число 4)
// выполняем 1 + 4, результат 5 посещаем в переменную "x"